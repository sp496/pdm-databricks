# Core Databricks / Spark (provided by the cluster at runtime)
# pyspark>=3.3.0
# delta-spark>=2.3.0

# Data processing
pandas>=1.5.0
numpy>=1.23.0
openpyxl>=3.1.0   # .xlsx workbooks (mapping files, SAP report)
xlrd>=2.0.1       # legacy .xls workbooks (e.g. Almac clinical inventory report)
